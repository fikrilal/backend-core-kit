#!/usr/bin/env bash
# Agent-only convenience wrapper around `tools/agent/win` for Windows npm.
#
# Usage:
# - `bash tools/agent/npmw ci`
# - `bash tools/agent/npmw run test:e2e`
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$script_dir/win" npm "$@"

