services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: backend_core_kit
      # Local-only convenience: do not require a password.
      # Never run this configuration in production.
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - '127.0.0.1:54321:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d backend_core_kit']
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    # Local-only convenience: no AUTH, bind to localhost via port mapping.
    # Never run this configuration in production.
    command: ['redis-server', '--appendonly', 'yes']
    ports:
      - '127.0.0.1:63790:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 20

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: ['server', '/data', '--console-address', ':9001']
    environment:
      # Local-only convenience: hard-coded credentials for dev/test.
      # Never run this configuration in production.
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - '127.0.0.1:59090:9000'
      - '127.0.0.1:59091:9001'
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  redis_data:
  minio_data:
